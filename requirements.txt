# Core runtime dependencies (pinned to compatible minor versions)
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0
Flask-Login>=0.6,<1.0
Flask-WTF>=1.2,<2.0
WTForms>=3.1,<4.0
email-validator>=2.1,<3.0
python-dotenv>=1.0,<2.0
SQLAlchemy>=2.0,<3.0

# Production WSGI servers (use whichever fits your platform)
gunicorn>=21.2; platform_system != "Windows"
waitress>=3.0; platform_system == "Windows"

# Optional drivers — uncomment for MySQL / PostgreSQL
# PyMySQL>=1.1
# psycopg[binary]>=3.1
